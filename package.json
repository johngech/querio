{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "name": "querio-monorepo",
  "scripts": {
    "build": "bun run build.ts",
    "test": "bun test",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.12",
    "@types/bun": "^1.3.14",
    "typeorm": "^0.3.24",
    "typescript": "5.9.3"
  }
}
